# 避難所登録画面を実装するためのプロンプト

以下の要件を満たす「避難所登録画面」を、既存システムに実装してください。

この指示は、避難所登録機能だけを他のシステムへ移行するためのものです。既存システムのホーム画面、ログイン、共通ヘッダー、ルーティング、データ保存方式などは、現在の構成を尊重してください。必要な範囲だけを変更し、無関係な機能やデザインは変更しないでください。

## 1. 実装前に行うこと

最初に、既存システムを確認してください。

- 避難所登録画面を表示しているテンプレートまたは画面コンポーネント
- 避難所の登録・更新・削除を処理しているサーバー側のルート、コントローラー、API
- 現在の避難所データの保存形式
- 共通ヘッダー、タイトルバー、ハンバーガーメニュー
- ログインや権限チェックの仕組み
- 既存の避難所検索画面や避難所一覧画面
- 既存データとの後方互換性

実装前に、現在の構成に合う最小限の変更方針を決めてください。既存のフレームワーク、命名規則、データアクセス方法、CSSの書き方を優先してください。

## 2. 画面レイアウト

避難所登録画面は、次の構成にしてください。

### 2.1 共通タイトルバー

画面上部に、既存システムの共通タイトルバーを表示してください。

- 左側にアプリケーション名または既存のタイトル
- 右側にハンバーガーメニュー
- ハンバーガーメニューを押すと、遷移可能な画面の一覧を表示
- 既存のホーム画面にハンバーガーメニューがある場合は、その実装とデザインを再利用
- メニュー項目には、少なくとも以下を含める
  - ホーム
  - 避難所検索
  - 避難所登録
  - 必要に応じて既存の管理画面やログアウト

避難所登録画面だけのために、共通ヘッダーを別実装しないでください。既存の共通部品がある場合は利用してください。

### 2.2 現在地表示

タイトルバーの下、画面左上にパンくず形式の現在地を表示してください。

例:

```text
ホーム > 避難所登録
```

- 「ホーム」はホーム画面へのリンク
- 「避難所登録」は現在画面を表すテキスト
- 将来階層が増えても拡張しやすい構造にする

### 2.3 左側: 入力フォーム

画面左側に避難所情報の入力フォームを配置してください。

入力項目は上から次の順番です。

1. 避難所名
2. 受け入れ済み人数
3. 受け入れ可能人数
4. 住所
5. 電話番号

各入力欄には、対応するラベルを表示してください。

#### 入力形式

- 避難所名: 文字列
- 受け入れ済み人数: 0以上の数値
- 受け入れ可能人数: 0以上の数値
- 住所: 文字列
- 電話番号: 数字のみ

電話番号については、次の両方を実装してください。

- 画面側で数字以外を入力できない、または入力された数字以外を自動的に除去
- サーバー側でも数字以外を拒否

画面側の制御だけに依存しないでください。

### 2.4 左側: 操作ボタン

入力フォームの下部に、次のボタンを配置してください。

- 削除
- 登録・更新

ボタンの役割は次のとおりです。

#### 登録・更新

- ラジオボタンが未選択の場合は、新規登録
- ラジオボタンが選択されている場合は、選択中の避難所を更新
- 登録または更新が成功したら、入力欄をすべてクリア
- 登録または更新が成功したら、ラジオボタンの選択も解除
- 登録または更新後の避難所一覧を再表示

#### 削除

- ラジオボタンが選択されていない場合は、避難所データを削除しない
- ラジオボタンが選択されている状態で1回目に押した場合、確認警告を表示
- 1回目では削除を実行しない
- 確認警告が表示されている状態で、もう一度「削除」を押した場合に削除を確定
- 削除後は入力欄をクリア
- 削除後はラジオボタンの選択を解除
- 削除対象を一覧から除去
- 選択なしで削除ボタンを押しても、入力欄を不用意にクリアしない

削除確認の状態は、画面側の表示だけでなく、サーバー側でも確認済みかどうかを判断できるようにしてください。例えば、確認用のhidden値、確認トークン、または既存システムに適した同等の仕組みを使ってください。

## 3. 右側: 避難所一覧

画面右側に、登録済み避難所の一覧を表示してください。

### 3.1 表示内容

- 各避難所の先頭にラジオボタン
- ラジオボタンの右側に避難所名
- 避難所名を表示
- 一覧は登録済みデータから生成

### 3.2 スクロール

避難所が増えても画面全体が過度に縦長にならないようにしてください。

- 一覧領域に最大高さを設定
- 最大高さを超えた場合は一覧領域だけ縦スクロール
- ページ全体のレイアウトが崩れないようにする

### 3.3 昇順ソート

避難所一覧は避難所名の昇順で表示してください。

次のすべてのタイミングでソートしてください。

- 初期表示時
- 新規追加後
- 更新後
- 削除後
- データ保存時

サーバー側でソートを保証してください。画面側のJavaScriptだけでソートしないでください。

文字コードや日本語の並び順については、既存システムで採用している方式を確認してください。単純な文字列比較で要件を満たせない場合は、対象言語・フレームワークで適切なロケール比較を検討してください。

## 4. ラジオボタン選択時の動作

避難所一覧のラジオボタンを押したら、対象避難所のデータを入力フォームへ自動入力してください。

自動入力する項目:

- 避難所名
- 受け入れ済み人数
- 受け入れ可能人数
- 住所
- 電話番号

また、選択中の避難所IDをフォーム送信時にサーバーへ渡せるようにしてください。

ラジオボタンを選択した状態で「登録・更新」を押した場合は、選択中のIDを使って既存データを更新してください。

## 5. 選択解除ボタン

「避難所一覧」の見出し右側に「選択解除」ボタンを追加してください。

選択解除ボタンの仕様:

- ラジオボタンが選択されている場合
  - 選択を解除
  - 入力フォームをクリア
  - 選択中の避難所IDをクリア
  - 新規登録できる状態に戻す
- ラジオボタンが1つも選択されていない場合
  - 何もしない
  - 入力フォームの入力内容を保持
  - 入力途中の新規避難所データを消さない

特に、ラジオボタン未選択状態は新規避難所の入力状態である可能性があります。未選択時の選択解除ボタン押下で入力内容を消去しないでください。

## 6. 受け入れ人数超過時の警告

受け入れ済み人数が受け入れ可能人数を超えた場合、警告を表示してください。

比較条件:

```text
受け入れ済み人数 > 受け入れ可能人数
```

### 6.1 初回送信

超過状態で「登録・更新」を初めて押した場合:

- 警告を表示
- 登録・更新処理は実行しない
- 入力内容を保持
- 選択中の避難所がある場合は選択状態を保持
- ユーザーに、もう一度「登録・更新」を押せば登録・更新できることを知らせる

警告文の例:

```text
受け入れ済み人数が受け入れ可能人数を超えています。もう一度「登録・更新」を押すと、この内容で登録・更新できます。
```

### 6.2 2回目の送信

警告表示後、入力内容を変更せずにもう一度「登録・更新」を押した場合:

- 超過状態でも登録または更新を実行
- 登録・更新成功後は入力欄と選択状態をクリア

### 6.3 警告後に入力内容を変更した場合

警告表示後に以下のいずれかを変更した場合、確認状態を無効にしてください。

- 避難所名
- 受け入れ済み人数
- 受け入れ可能人数
- 住所
- 電話番号

その状態で再度超過していれば、もう一度警告を表示してください。

確認状態をhidden値だけで管理する場合でも、サーバー側で必ず現在の入力値を再検証してください。クライアントから確認済みフラグが送られただけで無条件に保存しないでください。

## 7. データモデル

避難所データは、既存システムの保存方式に合わせてください。最低限、次の項目を保持してください。

```json
{
  "id": 1,
  "name": "避難所名",
  "accepted_count": "0",
  "capacity": "100",
  "address": "住所",
  "phone": "0123456789"
}
```

### 7.1 ID

- 新規登録時に既存IDと重複しないIDを発行
- 更新時は既存IDを維持
- 削除時は対象IDのデータだけを削除

### 7.2 既存データとの互換性

既存データに新しい項目が存在しない場合でも画面が壊れないようにしてください。

例:

```json
{
  "id": 2,
  "name": "既存避難所"
}
```

不足している項目は空欄として表示し、更新時に入力された値を保存してください。

### 7.3 数値の扱い

受け入れ済み人数と受け入れ可能人数は、既存システムの型に合わせてください。ただし、比較時には安全に数値化してください。

- 両方が有効な数値の場合だけ超過判定
- 空欄の場合は、既存システムの方針に合わせる
- 不正な値でサーバーエラーを起こさない
- 可能であれば0以上の入力制約を設定

## 8. サーバー側処理の要件

登録ルートまたはAPIでは、次の順序で処理してください。

1. フォームまたはリクエストから値を取得
2. 文字列の前後空白を適切に除去
3. 電話番号が数字のみか検証
4. 必須項目を検証
5. 操作が削除かどうかを判定
6. 削除の場合は、選択IDの有無と削除確認状態を確認
7. 登録・更新の場合は、受け入れ人数の数値化と超過判定
8. 超過かつ未確認なら、警告を返して保存しない
9. 新規登録または既存データ更新
10. 避難所名の昇順にソート
11. 保存
12. 成功後は空のフォーム状態で画面を表示

クライアント側のJavaScriptだけで、登録、更新、削除、超過確認、電話番号検証を完結させないでください。重要な処理はサーバー側でも検証してください。

## 9. 画面側の実装要件

画面側では次の状態を明確に扱ってください。

- 初期状態: ラジオボタン未選択、空の入力フォーム
- 新規登録入力中: ラジオボタン未選択、入力内容あり
- 既存避難所選択中: ラジオボタン選択、対象データが入力欄に表示
- 削除確認中: ラジオボタン選択、入力内容保持、警告表示
- 超過確認中: 入力内容保持、警告表示
- 登録・更新成功後: 入力欄空、ラジオボタン未選択
- 削除成功後: 入力欄空、ラジオボタン未選択、一覧から対象消失

警告や成功メッセージの表示場所は、入力フォームの上部など、ユーザーが見落としにくい場所にしてください。

## 10. UIとレスポンシブ対応

- デスクトップでは左に入力フォーム、右に避難所一覧
- 画面幅が狭い場合は、入力フォームと一覧を縦方向に配置
- 入力欄やボタンの文字がはみ出さないようにする
- ボタンの役割が視覚的に分かるようにする
- 一覧のスクロール領域を維持する
- 既存システムのデザインと統一する
- 既存の共通ヘッダー、色、余白、フォントを可能な限り再利用する

## 11. セキュリティと安全性

既存システムにログインや管理者権限がある場合、避難所登録画面にも同じ権限チェックを適用してください。

また、可能であれば以下を確認してください。

- CSRF対策
- 入力値のHTMLエスケープ
- IDの改ざん対策
- 存在しないIDへの更新・削除対策
- 不正な電話番号や数値の拒否
- 同時更新時のデータ破損対策
- ファイル保存時のエラー処理

既存システムにこれらの仕組みがない場合は、今回の避難所登録部分に必要な最小限の対策を追加してください。ただし、無関係な全体改修は避けてください。

## 12. テスト要件

実装後、少なくとも次のテストを行ってください。

### 12.1 表示

- 未ログイン時の権限動作が既存仕様どおり
- ログイン済みで避難所登録画面が表示される
- 入力項目が指定順で表示される
- 避難所一覧が表示される
- 避難所一覧が避難所名の昇順になっている
- 一覧が多い場合に一覧だけスクロールできる

### 12.2 新規登録

- ラジオボタン未選択状態で入力して登録できる
- 登録後に一覧へ追加される
- 登録後に一覧が昇順になる
- 登録後に入力欄がクリアされる
- 登録後に選択状態が解除される

### 12.3 更新

- ラジオボタン選択で対象情報が入力欄に表示される
- 登録・更新で対象データが上書きされる
- IDが変わらない
- 更新後に一覧が昇順になる
- 更新後に入力欄と選択状態がクリアされる

### 12.4 選択解除

- 選択中に選択解除すると入力欄がクリアされる
- 選択中に選択解除するとラジオボタンが解除される
- 未選択状態で選択解除しても入力途中の内容が消えない

### 12.5 削除

- 選択なしで削除してもデータが消えない
- 選択ありで1回目の削除を押すと警告が出る
- 1回目ではデータが消えない
- 2回目の削除で対象データが消える
- 削除後に一覧から消える
- 削除後に入力欄がクリアされる

### 12.6 人数超過

- 受け入れ済み人数が受け入れ可能人数以下なら通常登録できる
- 受け入れ済み人数が受け入れ可能人数を超えると警告が出る
- 初回の超過登録では保存されない
- 2回目の登録・更新で保存できる
- 警告後に入力値を変更すると再確認が必要になる

### 12.7 電話番号

- 数字だけの電話番号を登録できる
- 画面上で数字以外が除去または入力不可になる
- サーバーへ直接非数字を送っても保存されない

## 13. 実装時の進め方

次の順番で作業してください。

1. 既存の登録画面、登録処理、データ形式を確認
2. 既存のデータを壊さない形でデータ項目を拡張
3. サーバー側に新規登録・更新・削除・ソート処理を実装
4. 削除確認と人数超過確認をサーバー側に実装
5. 入力フォームと避難所一覧のレイアウトを実装
6. ラジオボタン選択によるフォーム自動入力を実装
7. 選択解除ボタンを実装
8. 電話番号の画面側・サーバー側検証を実装
9. 登録・更新・削除後のフォームクリアを実装
10. テストを実行
11. 構文エラー、テンプレートエラー、差分エラーを確認
12. 変更したファイル、実装内容、テスト結果を簡潔に報告

## 14. 完了条件

次の条件をすべて満たしたら完了としてください。

- 避難所登録画面が左右レイアウトで表示される
- 共通タイトルバーとハンバーガーメニューが利用できる
- パンくずが表示される
- 入力項目が5つある
- ラジオボタンで既存情報を入力欄へ反映できる
- ラジオボタン未選択時に新規登録できる
- ラジオボタン選択時に更新できる
- 選択解除ボタンが選択中だけ入力欄をクリアする
- 登録・更新後に入力欄と選択状態がクリアされる
- 削除は2回押下で確定する
- 削除後に一覧から対象が消える
- 一覧が避難所名の昇順になる
- 人数超過時に警告が表示される
- 警告後の再押下で超過状態を登録・更新できる
- 電話番号に数字以外を保存できない
- 既存データが壊れない
- テストと構文確認が成功する

上記を満たすために必要なファイルを編集し、実際に動作する状態まで実装してください。質問や懸念点がある場合でも、まず既存コードから合理的に判断して実装を進め、最後に未解決事項だけを報告してください。
